/**
 * test_globals.cpp — stub: globals are no longer used.
 * Kept as a compilation unit so the test target doesn't break.
 * New tests use VfsFixture from test_helpers.h.
 */

/* Intentionally empty — all state is now in class instances. */
